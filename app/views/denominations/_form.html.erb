<%= form_with(model: denomination, local: true) do |form| %>
  <% if denomination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(denomination.errors.count, "error") %> prohibited this denomination from being saved:</h2>

      <ul>
      <% denomination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :denomination_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
